import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    paddingVertical: 32,
    alignItems: "center",
    gap: 12,
  },
  title: {
    fontSize: 16,
    fontWeight: "700",
    color: "#DC2626",
  },
  message: {
    fontSize: 14,
    color: "#64748B",
    textAlign: "center",
  },
});
